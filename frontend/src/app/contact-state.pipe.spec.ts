import { ContactStatePipe } from './contact-state.pipe';

describe('ContactStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ContactStatePipe();
    expect(pipe).toBeTruthy();
  });
});
